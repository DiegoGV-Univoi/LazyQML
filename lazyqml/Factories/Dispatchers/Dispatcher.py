def dispatch(nqubits, randomstate, predictions, ignoreWarnings,
                 numPredictors, numLayers, classifiers, ansatzs,
                 embeddings, features, learningRate, epochs, runs,
                 maxSamples, verbose, customMetric, customImputerNum, customImputerCat):
    
    

    pass
